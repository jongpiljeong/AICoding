# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Status

This project is in its initial state. Only a `.env` file is present — no source code or build system has been set up yet.

## Environment

- `.env` contains an `OPENROUTER_API_KEY` for use with the OpenRouter API (access to various LLM models via a unified OpenAI-compatible endpoint). The OpenRouter API is OpenAI-compatible, so use the `openai` SDK pointed at `https://openrouter.ai/api/v1`.

## Next Steps

Update this file once the project takes shape: add build/test/lint commands, runtime/language choice, and architecture overview.
