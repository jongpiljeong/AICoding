# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Environment

- **OpenRouter API key** is available via `process.env.OPENROUTER_API_KEY` (or `OPENROUTER_API_KEY` in `.env`)
- OpenRouter base URL: `https://openrouter.ai/api/v1` (OpenAI-compatible API)
